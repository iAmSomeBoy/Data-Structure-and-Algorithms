#include <bits/stdc++.h>

int main() {
    // Seed with current time (most common method)
    srand(time(0));
    
    // Now rand() will produce different sequences
    std::cout << "Random number: " << rand() << std::endl;
    
    return 0;
}